# UI Service (React)

This app implements the user-facing UI and CLI for the modernized accounting system.

Quick start:
- Set REACT_APP_API_BASE_URL in a `.env` file
- npm install
- npm start

Docs:
- See README-UIService.md for features, CLI usage, and accessibility/testing notes.
